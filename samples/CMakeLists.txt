INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/src )

SET(example3_SRCS example3.c)
ADD_DEFINITIONS(${REDLAND_CFLAGS})
ADD_EXECUTABLE(example3 ${example3_SRCS})
TARGET_LINK_LIBRARIES(example3 ${REDLAND_LIBRARIES})

SET(qexample3_SRCS qexample3.cpp)
INCLUDE_DIRECTORIES(${QT_QTCORE_INCLUDE_DIR} ${QT_INCLUDE_DIR})
ADD_DEFINITIONS(${REDLAND_CFLAGS})
ADD_EXECUTABLE(qexample3 ${qexample3_SRCS})
TARGET_LINK_LIBRARIES(qexample3 ${REDLAND_LIBRARIES})
TARGET_LINK_LIBRARIES(qexample3 ${QT_QTCORE_LIBRARIES})
TARGET_LINK_LIBRARIES(qexample3 qrdf)

######################### DISABLED #########################

#Set (example1_SRCS example1.c)
#ADD_DEFINITIONS(${REDLAND_CFLAGS})
#ADD_EXECUTABLE(example1 ${example1_SRCS})
#TARGET_LINK_LIBRARIES(example1 ${REDLAND_LIBRARIES})

#SET(crashc_SRCS crash-c.c)
#ADD_DEFINITIONS(${REDLAND_CFLAGS})
#ADD_EXECUTABLE(crashc ${crashc_SRCS})
#TARGET_LINK_LIBRARIES(crashc ${REDLAND_LIBRARIES})

#SET(crashcpp_SRCS crash-cpp.cpp)
#ADD_DEFINITIONS(${REDLAND_CFLAGS})
#ADD_EXECUTABLE(crashcpp ${crashcpp_SRCS})
#TARGET_LINK_LIBRARIES(crashcpp ${REDLAND_LIBRARIES})

#SET(qexample1_SRCS qexample1.cpp)
#INCLUDE_DIRECTORIES(${QT_QTCORE_INCLUDE_DIR})
#ADD_EXECUTABLE(qexample1 ${qexample1_SRCS})
##ADD_DEPENDENCIES(qexample1 qrdf)
#FIND_LIBRARY(QRDF_LIBRARY NAMES qrdf PATHS ${CMAKE_BINARY_DIR}/src)
#TARGET_LINK_LIBRARIES(qexample1 qrdf)
#TARGET_LINK_LIBRARIES(qexample1 ${RAPTOR_LIBRARIES})
#TARGET_LINK_LIBRARIES(qexample1 ${RASQAL_LIBRARIES})
#TARGET_LINK_LIBRARIES(crashcpp ${REDLAND_LIBRARIES})
#TARGET_LINK_LIBRARIES(qexample1 ${QT_QTCORE_LIBRARIES})

###################### INSTALL #########################

install(TARGETS example3
		qexample3
		RUNTIME DESTINATION bin
		LIBRARY DESTINATION lib)
