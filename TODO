Soprano 2 TODO List

Add here what you want to add in Soprano

* ATM Soprano::Plugin does not inherit QObject but in PluginManager we do a qobject_cast, thus we need
  plugins to be QObjects. Is that necessary and if so why does Plugin not inherit Qobject directly?
